services:
  cubemap:
    image: falconbi/tm1-cubemap:latest
    ports:
      - "8084:8084"
    volumes:
      - ./servers.json:/app/servers.json
      - tm1_data:/app/cube_map
    restart: unless-stopped

volumes:
  tm1_data:
